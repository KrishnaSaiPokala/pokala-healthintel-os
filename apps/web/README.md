# Web App

Elite SaaS shell for Pokala HealthIntel OS.

```bash
npm install
npm run dev
```

This app currently reads `src/data/intelligence.json`, generated by the overnight runner.
Next upgrade: replace JSON loads with curated Parquet + DuckDB-WASM.
